<Tip>
Pass `model` in your request body to choose the transcription model:

- `"solaria-3"` — our latest model (best-in class on EU languages)
- `"solaria-1"` — our generalist model (code switching, async and real-time 100 languages)

If omitted, the API uses the default model.
</Tip>
