<Tip>
Pass `model` in your pre-recorded request body to choose the transcription model:

- `"solaria-3"` — our latest model (best-in class on EU languages)
- `"solaria-1"` — our generalist model (code switching, 100 languages)

If omitted, the API uses the default model.
</Tip>
